# Example Gemfile for testing update-versions inlay hints
source 'https://rubygems.org'

ruby '~> 3.2'

# Web framework
gem 'rails', '~> 7.1'
gem 'puma', '>= 5.6'

# Database
gem 'pg', '~> 1.5'
gem 'redis', '~> 5.0'

# Auth / Security
gem 'devise', '~> 4.9'
gem 'pundit', '>= 2.3.0'

# API
gem 'jsonapi-serializer', '~> 2.2'

group :development, :test do
  gem 'rspec-rails', '~> 6.0'
  gem 'factory_bot_rails', '>= 6.2.0'
  gem 'faker', '~> 3.1'
end

group :development do
  gem 'rubocop', '~> 1.50'
  gem 'rubocop-rails', '>= 2.19.0'
end

# No version constraint — skipped by the provider
gem 'bootsnap'

# Git source — skipped by the provider
gem 'rack', git: 'https://github.com/rack/rack'
